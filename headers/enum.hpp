#ifndef ENUM_HPP
#define ENUM_HPP
#include <string>
#include <cstdint>

extern int PRIORITY[];

extern std::string OPERTEXT[32];

enum OPERATOR {
	LBRACKET, RBRACKET,
	COMMA,
	FUNCTION, RETURN, ENDFUNCTION,
	IF, ELSE,
	WHILE, ENDLOOP,
	GOTO, ASSIGN, COLON,
	OR,
	AND,
	BITOR,
	XOR,
	BITAND,
	EQ, NEQ,
	SHL, SHR,
	LEQ, LT,
	GEQ, GT,
	PLUS, MINUS,
	MULT, DIV, MOD,
	UNARY_MINUS // never produced by the lexer, MINUS is turned into it
};

enum ERRORS {
	ERR_WITH_GOTO_OR_LABELS,
	ERR_NOT_BALANCED_BRACKETS,
	ERR_NOT_BALANCED_FUNCTION,
	ERR_NOT_BALANCED_CONDITIONAL_OPERATORS,
	ERR_NAME_FUNCTION,
	ERR_FUNCTION_START_VARS,
	ERR_WITH_CONDITIONAL_OPERATORS,
	ERR_UNDEFINED_MAIN,
	ERR_UNDEFINED_VARIABLE,
	ERR_UNDEFINED_FUNCTION,
	ERR_ASSIGN_PROBLEMS,
	ERR_NO_COLONS_IN_CONDITION,
	ERR_WRONG_POSITION_FUNCTION,
	INVALID_SYNTAX,
	ERR_VOID_FUNCTION_IN_EVALUATION,
	ERR_DIVISION_BY_ZERO,
	ERR_NOT_ENOUGH_OPERANDS,
	ERR_EXTRA_VALUES_IN_LINE,
	ERR_INTEGER_OVERFLOW,
	ERR_BAD_SHIFT_COUNT
};

enum {UNDEFINED = -INT32_MAX};

enum {FUNCTION_RETURN = -1};

#endif